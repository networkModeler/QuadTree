////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// point.h : header file for 2D point class
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#ifndef POINT_H
#define POINT_H

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class Point
{
    public:

        // Constructor
        Point(int x, int y);

        // Get the point's coordinates
        int getX();
        int getY();

        // Calculate the distance
        double distance(Point* otherPoint);
        double distance(int x, int y);
        double distance(int xMin, int yMin, int xMax, int yMax);

        // Print
        void print();

    private:

        int x;
        int y;
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#endif  POINT_H